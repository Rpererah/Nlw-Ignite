--primeiro iniciamos com npm init para iniciar o package json
--Instalamos o fastify para entendermos como que se da a instalacao de uma biblioteca usando node 
--depois instalamos o typescript como dependecia de desenvolvimento usando npm i typescript -D
--chamamos o configurador do ts usando npx tsc --init
--npx i tsx -D essa dependecia pode executar o ts